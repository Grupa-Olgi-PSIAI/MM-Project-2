{% extends 'base.html.twig' %}

{% block body %}
    <h1>Attendance</h1>

    <table>
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ attendance.id }}</td>
        </tr>
        <tr>
            <th>Version</th>
            <td>{{ attendance.version }}</td>
        </tr>
        <tr>
            <th>Datecreated</th>
            <td>{% if attendance.dateCreated %}{{ attendance.dateCreated|date('Y-m-d H:i:s') }}{% endif %}</td>
        </tr>
        <tr>
            <th>Lastupdated</th>
            <td>{% if attendance.lastUpdated %}{{ attendance.lastUpdated|date('Y-m-d H:i:s') }}{% endif %}</td>
        </tr>
        <tr>
            <th>Timein</th>
            <td>{% if attendance.timeIn %}{{ attendance.timeIn|date('Y-m-d H:i:s') }}{% endif %}</td>
        </tr>
        <tr>
            <th>Timeout</th>
            <td>{% if attendance.timeOut %}{{ attendance.timeOut|date('Y-m-d H:i:s') }}{% endif %}</td>
        </tr>
        <tr>
            <th>Notes</th>
            <td>{{ attendance.notes }}</td>
        </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('attendances_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('attendances_edit', { 'id': attendance.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
            <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
