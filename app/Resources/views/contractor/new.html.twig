{% extends 'base.html.twig' %}

{% block top %}
    {{ include('nav/top.html.twig', {
        'title': 'Dodawanie kontrahenta'
    }) }}
{% endblock %}

{% block body %}
    {{ form_start(form) }}
    <div class="material-input">
        {{ form_widget(form) }}
    </div>
    <input class="material-btn" type="submit" value="Utwórz"/>
    {{ form_end(form) }}

    <a class="material-btn" href="{{ path('contractor_index') }}">Powrót do listy</a>
{% endblock %}
