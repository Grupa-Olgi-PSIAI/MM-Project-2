{% extends 'base.html.twig' %}

{% block top %}
    {{ include('nav/top.html.twig', {
        'title': 'Godziny pracy',
        'add': path('attendance_new')
    }) }}
{% endblock %}

{% block body %}
    <div>
        {{ knp_pagination_filter(pagination, {'attendance.timeIn' : 'Czas wejścia', 'attendance.timeOut' : 'Czas wyjścia'}) }}
    </div>
    <table>
        <thead>
        <tr>
            <th>{{ knp_pagination_sortable(pagination, 'Czas wejścia', 'attendance.timeIn') }}</th>
            <th>{{ knp_pagination_sortable(pagination, 'Czas wyjścia', 'attendance.timeOut') }}</th>
            <th>Notes</th>
            <th>Actions</th>
        </tr>
        </thead>
        <tbody>
        {% for attendance in pagination %}
            <tr>
                <td>{% if attendance.timeIn %}{{ attendance.timeIn|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if attendance.timeOut %}{{ attendance.timeOut|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ attendance.notes }}</td>
                <td>
                    <ul>
                        <li>
                            <a class="material-btn" href="{{ path('attendance_show', { 'id': attendance.id }) }}">show</a>
                        </li>
                        <li>
                            <a class="material-btn" href="{{ path('attendance_edit', { 'id': attendance.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    <div>
        {{ knp_pagination_render(pagination) }}
    </div>
{% endblock %}
